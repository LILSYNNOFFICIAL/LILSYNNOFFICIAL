(()=>{'use strict';const ready=()=>{const feed=document.getElementById('releaseFeed');if(!feed||feed.dataset.releaseLimitReady)return;feed.dataset.releaseLimitReady='1';const trim=()=>{const cards=[...feed.children].filter(x=>x.matches('.release-card'));const seen=new Set;cards.forEach(card=>{const t=card.querySelector('.release-title')?.textContent?.trim()||'';if(!t||seen.has(t)||[...feed.children].indexOf(card)>=9)card.remove();else seen.add(t)});};new MutationObserver(trim).observe(feed,{childList:true});trim()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ready,{once:true});else ready()})();