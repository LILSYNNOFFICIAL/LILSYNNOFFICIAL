(()=>{'use strict';
const ready=()=>{const feed=document.getElementById('releaseFeed');if(!feed||feed.dataset.releaseLimitReady)return;if(!document.querySelector('.release-feed'))return;feed.dataset.releaseLimitReady='1';const style=document.createElement('style');style.id='ls-release-limit-css';style.textContent='.release-feed{grid-template-columns:repeat(4,minmax(0,1fr))!important;gap:9px!important}.release-feed .release-card{min-width:0!important}.release-feed .release-art{width:100%!important;aspect-ratio:1/1!important;max-height:220px!important;object-fit:cover!important}@media(max-width:900px){.release-feed{grid-template-columns:repeat(2,minmax(0,1fr))!important}.release-feed .release-art{max-height:180px!important}}@media(max-width:520px){.release-feed{grid-template-columns:1fr 1fr!important;gap:7px!important}.release-feed .release-art{max-height:170px!important}}';document.head.appendChild(style);const trim=()=>{const seen=new Set;[...feed.children].forEach(card=>{const title=card.querySelector('.release-title')?.textContent?.trim()||card.querySelector('img')?.alt?.replace(/ artwork$/i,'').trim()||'';if(!title)return;if(seen.has(title))card.remove();else seen.add(title)});const cards=[...feed.children].filter(x=>x.matches('.release-card'));cards.slice(8).forEach(x=>x.remove())};new MutationObserver(trim).observe(feed,{childList:true});trim()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ready,{once:true});else ready();})();
