const assert = require('node:assert/strict');
const fs = require('node:fs');
const vm = require('node:vm');
const source=fs.readFileSync('command/designer.js','utf8');
const sandbox={console,localStorage:{setItem(){},getItem(){return null}},window:{},document:{readyState:'loading',addEventListener(){}}};
vm.createContext(sandbox);vm.runInContext(source,sandbox);
assert.deepEqual(sandbox.window.LSDesignerTestHooks.requiredIds,['stage','pageFrame','layers','inspector','status','viewportSelect','zoom']);
assert.equal(sandbox.window.LSDesignerTestHooks.historyRoundTrip(),true);assert.equal(sandbox.window.LSDesignerTestHooks.patchRoundTrip(),true);assert.equal(sandbox.window.LSDesignerTestHooks.draftRoundTrip(),true);assert.equal(sandbox.window.LSDesignerTestHooks.toolRegistryValid(),true);
const model=require('./designer-dom-model.js');assert.equal(model.version,1);assert.equal(typeof model.applyMove,'function');assert.equal(typeof model.canMove,'function');
const plusSource=fs.readFileSync('command/designer-plus.js','utf8');vm.runInContext(plusSource,sandbox);assert.equal(sandbox.window.LSDesignerPlus.version,1);assert.equal(typeof sandbox.window.LSDesignerPlus.addElement,'function');assert.equal(typeof sandbox.window.LSDesignerPlus.move,'function');assert.equal(typeof sandbox.window.LSDesignerPlus.align,'function');
const assetSource=fs.readFileSync('command/designer-assets.js','utf8');vm.runInContext(assetSource,sandbox);assert.equal(sandbox.window.LSDesignerAssets.version,2);assert.equal(typeof sandbox.window.LSDesignerAssets.insertAsset,'function');assert.equal(typeof sandbox.window.LSDesignerAssets.duplicateDom,'function');assert.equal(typeof sandbox.window.LSDesignerAssets.deleteDom,'function');
const structureSource=fs.readFileSync('command/designer-structure.js','utf8');assert.doesNotThrow(()=>new Function(structureSource));assert.match(structureSource,/semanticDrop/);assert.match(structureSource,/materialize-move/);assert.match(structureSource,/ls-drop-indicator/);
const persistSource=fs.readFileSync('command/designer-persist.js','utf8');vm.runInContext(persistSource,sandbox);assert.equal(sandbox.window.LSDesignerPersist.version,2);assert.equal(typeof sandbox.window.LSDesignerPersist.save,'function');assert.equal(typeof sandbox.window.LSDesignerPersist.serialize,'function');assert.equal(typeof sandbox.window.LSDesignerPersist.markCreated,'function');assert.equal(typeof sandbox.window.LSDesignerPersist.getStructure,'function');
const advancedSource=fs.readFileSync('command/designer-advanced.js','utf8');assert.doesNotThrow(()=>new Function(advancedSource));assert.match(advancedSource,/copyStyle/);assert.match(advancedSource,/pasteStyle/);assert.match(advancedSource,/textField/);
const html=fs.readFileSync('command/designer.html','utf8');assert.match(html,/designer-dom-model\.js/);assert.match(html,/designer-structure\.js/);assert.match(html,/designer-plus\.js/);assert.match(html,/designer-assets\.js/);assert.match(html,/designer-persist\.js/);assert.match(html,/designer-advanced\.js/);assert.match(html,/id="saveDom"/);assert.match(html,/LIL SYNN \/ DESIGNER/);assert.match(html,/Drop center = INSIDE/);assert.match(html,/edges = BEFORE\/AFTER/);
const api=fs.readFileSync('api/designer-dom.js','utf8');assert.match(api,/SAVE DOM/);assert.match(api,/LS-DESIGNER-DOM:START/);assert.match(api,/LS-DESIGNER-HIDE:START/);assert.match(api,/SAFE_TAGS/);assert.match(api,/parentId/);assert.doesNotMatch(api,/innerHTML/);
console.log('designer contracts: PASS');
