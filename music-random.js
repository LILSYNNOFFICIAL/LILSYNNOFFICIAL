const initLilSynnMusicRandom=()=>{const grid=document.getElementById('music-grid');if(!grid)return;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const shuffle=a=>{const out=[...a];for(let i=out.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[out[i],out[j]]=[out[j],out[i]]}return out};const key=s=>String(s||'').toLowerCase().normalize('NFKD').replace(/[’']/g,'').replace(/[^a-z0-9]/g,'');let pool=[];const render=items=>{grid.innerHTML=items.map(s=>`<article class="music-card"><a href="${s.spotify}" target="_blank" rel="noopener noreferrer"><img src="${s.art}" alt="${esc(s.trackName)} — LIL SYNN artwork" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='${s.fallbackArt}'"></a><div class="music-card-body"><h3 class="music-card-title">${esc(s.trackName)}</h3><p class="music-card-meta">${esc(s.collectionName)}${s.year?' · '+s.year:''}</p><div class="music-card-links"><a href="${s.spotify}" target="_blank" rel="noopener noreferrer">SPOTIFY</a><a href="${s.apple}" target="_blank" rel="noopener noreferrer">APPLE</a></div></div></article>`).join('')};const freshRandom=()=>{if(!pool.length)return;const count=Math.min(8,pool.length),previous=JSON.parse(sessionStorage.getItem('lilSynnMusicLastSet')||'[]');let chosen=[];for(let attempt=0;attempt<12;attempt++){const candidate=shuffle(pool).slice(0,count),signature=candidate.map(x=>key(x.trackName)).sort().join('|'),previousSignature=[...previous].sort().join('|');if(signature!==previousSignature||pool.length<=count){chosen=candidate;break}}if(!chosen.length)chosen=shuffle(pool).slice(0,count);sessionStorage.setItem('lilSynnMusicLastSet',JSON.stringify(chosen.map(x=>key(x.trackName))));render(shuffle(chosen))};window.lilSynnRefreshMusic=freshRandom;(async()=>{try{const stamp=Date.now();const [appleResponse,catalogResponse,artResponse]=await Promise.all([fetch(`https://itunes.apple.com/lookup?id=1850720041&entity=song&limit=200&_=${stamp}`,{cache:'no-store'}),fetch(`/release-catalog.json?music_refresh=${stamp}`,{cache:'no-store'}),fetch(`https://api.github.com/repos/LILSYNNOFFICIAL/LILSYNNOFFICIAL/contents/assets/images/icons/album_art?ref=main&_=${stamp}`,{cache:'no-store'})]);if(!appleResponse.ok||!catalogResponse.ok)throw Error('Catalog unavailable');const appleData=await appleResponse.json(),catalog=await catalogResponse.json();const appleSongs=(appleData.results||[]).filter(x=>x.wrapperType==='track'&&x.kind==='song'&&String(x.artistName||'').toLowerCase()==='lil synn'&&x.trackName&&x.trackViewUrl),appleByTitle=new Map(appleSongs.map(x=>[key(x.trackName),x])),spotifyMap=catalog.trackSpotify||{};let artFiles=[];if(artResponse.ok){const j=await artResponse.json();artFiles=(Array.isArray(j)?j:[]).filter(x=>x.type==='file'&&/\.(jpg|jpeg|png)$/i.test(x.name));}const artFor=release=>{const k=key(release);let f=artFiles.find(x=>key(x.name.replace(/^\d+_lil_synn_/i,'').replace(/\.(jpg|jpeg|png)$/i,''))===k);if(!f)f=artFiles.find(x=>{const n=key(x.name.replace(/\.(jpg|jpeg|png)$/i,''));return n.includes(k)||k.includes(n)});return f?`https://raw.githubusercontent.com/LILSYNNOFFICIAL/LILSYNNOFFICIAL/main/assets/images/icons/album_art/${encodeURIComponent(f.name)}`:''};const entries=[];for(const release of catalog.order||[]){const group=catalog.groups?.[release];if(group?.tracks){for(const track of group.tracks)entries.push({track,release})}else if(key(release)!=='touchingtothenorth')entries.push({track:release,release})}const seen=new Set();for(const e of entries){const a=appleByTitle.get(key(e.track)),spotify=spotifyMap[e.track];if(!a||!spotify||seen.has(key(a.trackName)))continue;seen.add(key(a.trackName));const art=artFor(e.release)||a.artworkUrl100?.replace('100x100bb','600x600bb')||'';pool.push({trackName:a.trackName,collectionName:e.release,apple:a.trackViewUrl,spotify,art,fallbackArt:a.artworkUrl100?.replace('100x100bb','600x600bb')||art,year:a.releaseDate?new Date(a.releaseDate).getFullYear():''})}freshRandom();}catch(e){console.warn('Release-derived random music unavailable',e)}})()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initLilSynnMusicRandom,{once:true});else initLilSynnMusicRandom();
