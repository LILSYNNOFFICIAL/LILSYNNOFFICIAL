// Deprecated homepage runtime intentionally neutralized.
// The canonical homepage background/layout is owned by /homepage-final-fix.js.
// Keeping this compatibility file avoids a stale browser request while preventing
// the legacy hero WebM positioning rules from overriding the fixed LS_BG_STARS design.
(() => {})();
